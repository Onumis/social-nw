<% flash.each do |name, msg| %>
  <div class="alert <%= flash_to_bs_alert(name) %>">
    <button type="button" class="close" "data-dismiss"="alert" "aria-hidden"="true">
      &times;
    </button>
    <%= msg %>
  </div>
<% end %>